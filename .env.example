# ─── Frontend (Vite) ─────────────────────────────────────────────
# These are exposed to the browser via import.meta.env
# The anon key is safe for client-side use (rate-limited, RLS-protected)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-supabase-anon-key
VITE_SUPABASE_PROJECT_ID=your-project-ref

# ─── Edge Function Secrets (set via Supabase CLI, NOT here) ──────
# These must NEVER be committed or exposed to the frontend.
# Set them with: supabase secrets set KEY=value
#
# ELEVENLABS_API_KEY=your-elevenlabs-key
# MINIMAX_API_KEY=your-minimax-key
# GROQ_API_KEY=your-groq-key
#
# The following are auto-injected by Supabase into edge functions:
# SUPABASE_URL (auto-injected)
# SUPABASE_SERVICE_ROLE_KEY (auto-injected)
